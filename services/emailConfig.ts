// services/emailConfig.ts
// IMPORTANT: Replace these placeholder values with your actual EmailJS credentials.
// You can get these from your EmailJS account dashboard.
// The user provided a Gmail app password (hnlr elur pvdf immz), which should be used
// to set up the EmailJS service for Gmail.
export const emailConfig = {
  serviceId: 'YOUR_SERVICE_ID', // e.g., 'service_abc123'
  templateId: 'YOUR_TEMPLATE_ID', // The ID of the generic email template you create in EmailJS
  publicKey: 'YOUR_PUBLIC_KEY', // e.g., 'user_xyz456'
};
